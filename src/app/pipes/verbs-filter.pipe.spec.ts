import { VerbsFilterPipe } from './verbs-filter.pipe';

describe('VerbsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VerbsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
